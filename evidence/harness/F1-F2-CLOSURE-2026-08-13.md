# F1 + F2 — Pre-Cutover Corrections CLOSURE (Stage 6.6 → Stage 7 gate, 13 Aug 2026)

## Status: F1 PASS ✅ + F2 PASS ✅ → Stage 7 AUTHORIZED (per Founder: "After F1 + F2 PASS → STAGE 7 PRODUCTION CUTOVER GO")

---

## F1 — Silent Skill Self-Improvement Recurrence: CLOSED

**Root cause (3 layers, all confirmed):**
1. `skills.write_approval: False` (default) — no approval gate on skill_manage writes
2. Memory-review fork (spawned ~every 10 turns, prompt "update the skill library. Be ACTIVE") had full skill_manage authority
3. Session `20260812_173534_4700a7` booted 17:35 — BEFORE Stage 4.1's `creation_nudge_interval: 0` → held stale config for its whole life

**Fork actions found (01:45–02:04):** created `notebooklm-deep-research` skill (02:03, quality content — R4 flow + cp1252 pitfall + CDP pattern), patched `cross-profile-governance-sync` (reference line), created `references/model-routing-migration-sweep-2026-08-13.md`. model-routing patch attempts were REFUSED (not curator-managed — built-in guard worked).

**Fix:**
- `skills.write_approval: true` on **13 production profiles** (iip + 11 org-* + ipm) — skill_manage writes now STAGED for Founder review (`/skills diff` → `/skills approve`/`reject`); applies to BOTH foreground and background fork (config_defaults L1891-1902)
- `creation_nudge_interval: 0` confirmed on all 13 (found 4 profiles — org-macro-strategist / org-options-strategist / org-radar-scout / ipm — still at 15, never preflighted → fixed)
- Fork mutations REVERTED → candidates preserved at `evidence/harness/f1-fork-candidates/`
- Config backups: `*.bak-2026-08-13-f1` all 13 profiles

**Verification (bounded trigger-cross):** 20 tool iterations (crosses old threshold 15) → fork spawned → **ZERO canonical skill mutation** (hashes unchanged: cross-profile `73cc286f`, llm-council `7b0a8990`; notebooklm-deep-research absent). `hermes skills list-modified` = clean.

## F2 — AGENTS Research-Skill Routing: CLOSED

- AGENTS.md research row now references ONLY installed/approved skills: `fundamental-company-research`, `sec-edgar-research`, `iip-evidence`, `iip-discovery-audit`, `iip-editorial-publication`, `iip-hermes-workforce` + canonical contracts (Evidence Model, Discovery Recall v1.1)
- Stale proposed names REMOVED (no `iip-deep-research` / `iip-publication` / `capital-kanban` — never created, now never referenced)
- Rule: "Research tasks load only the task-relevant APPROVED and INSTALLED research skills/contracts" — no universal research-skill loading
- **Thin skills PROMOTED (Founder-sanctioned, Stage 5+6 proof):** `iip-evidence` v1.0.0 + `iip-discovery-audit` v1.0.0 — created on iip, synced to 13 profiles; kept thin (no proliferation)
- Commit: `f1ec9ec` (main repo AGENTS.md) · `9a57cc0` (harness evidence)

## NotebookLM operational note (recorded, not blocking)

- `browser_exec` stdout = defective on Windows Unicode (cp1252) — direct CDP websocket is the proven working transport (captured in f1-fork-candidates for review; may be promoted into a skill after Founder review)

## Stage 7: GO

All pre-cutover checks GREEN: P1 (premium execution path) · P2 (AGENTS trigger routing) · R4 (NotebookLM transport) · F1 (skill-mutation guard) · F2 (research routing = installed reality). Proceed to Stage 7 per Founder's 7.0–7.8 order. **STOP before Stage 8 (old board deletion).**

---
<!-- 2026-08-13 02:18:47 +0700 — captured via scripts/artifact_timestamp.py (system clock at write) -->
