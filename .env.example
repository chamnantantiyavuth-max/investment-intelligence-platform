# IIP Backend environment (Real-Data Production Path, FD #46)
# Copy to .env and fill real values. NEVER commit .env (gitignored).
#
# Auth (required at boot — fail-fast guards, arch v0.4 §5):
IIP_AUTH_USER=founder
IIP_AUTH_PASSWORD=change-me-strong-password
IIP_AUTH_SECRET=replace-with-40-plus-random-chars

# Optional overrides (defaults are repo-relative):
# IIP_DB_PATH=backend/data/iip.db
# IIP_ARTIFACT_BASE=<dir containing am/ fo/ ii/ subdirs>
